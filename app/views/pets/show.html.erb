<h2><%= @pet.name %></h2>

<p><%= @pet.species %></p>
<p><%= @pet.address %></p>
<p><%= @pet.found_on %></p>

<%= link_to "Edit!", edit_pet_path(@pet) %>

<%= link_to "Delete!", pet_path(@pet), method: :delete %>

<%= link_to "See them pets", pets_path %>
